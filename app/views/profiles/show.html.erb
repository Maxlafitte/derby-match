<div>
  <div>
    <% if @user.photo? %>
      <%= cl_image_tag @user.photo, width: 200, height: 200, crop: :thumb, gravity: :face %>
    <% else %>
      <%= image_tag 'avatar.png' %>
    <% end %>
  </div>

  <div>
    <h2><%= @user.derby_name %></h2>
    <p><%= @user.email %></p>
<!-- ajouter user.team.name quand la liste des equipes sera faite et integrée dans le sign up -->
  </div>
</div>
